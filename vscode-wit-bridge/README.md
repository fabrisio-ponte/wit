# Wit Bridge VS Code Extension

This extension sends simple workspace events to the local wit CLI.

## Commands

- Wit: Start Session
- Wit: Record Event
- Wit: Commit Session
- Wit: Status

## Configuration

- `wit.cliPath` defaults to `./wit`

If your CLI is installed globally, set `wit.cliPath` to `wit`.
